import type { DecodedFood } from '../api/foodApi'
export function FoodDetails({ food }: { food: DecodedFood }) { if (!food.ingredients && !food.commonPortion) return null; return <details className="rounded-[28px] border border-slate-200 bg-white px-6 py-5 shadow-sm"><summary className="cursor-pointer font-semibold text-slate-900">Source details</summary><div className="mt-4 space-y-4 text-sm text-slate-700">{food.ingredients ? <div><h2 className="font-semibold text-slate-950">Ingredient statement</h2><p className="mt-1 leading-6">{food.ingredients}</p></div> : null}{food.commonPortion ? <div><h2 className="font-semibold text-slate-950">Reference portion</h2><p className="mt-1">{food.commonPortion.description} ({food.commonPortion.grams} g). Nutrition values above remain standardized to 100 g.</p></div> : null}</div></details> }
