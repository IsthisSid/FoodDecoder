export function IngredientsOfInterest({ ingredients }: { ingredients: string[] }) { if (!ingredients.length) return null; return <section className="rounded-[32px] border border-amber-200 bg-amber-50 p-6 sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.22em] text-amber-800">Ingredients of interest</p><ul className="mt-4 grid gap-2 text-slate-900 sm:grid-cols-2">{ingredients.map((ingredient) => <li key={ingredient} className="rounded-xl bg-white px-4 py-3 font-medium shadow-sm">{ingredient}</li>)}</ul></section> }
