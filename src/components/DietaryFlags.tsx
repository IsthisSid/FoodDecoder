import type { DietaryResult } from '../api/foodApi'
const labels = { vegetarian: 'Vegetarian', vegan: 'Vegan', dairyFree: 'Dairy free', glutenFree: 'Gluten free', eggFree: 'Egg free', soyFree: 'Soy free' } as const
export function DietaryFlags({ dietary }: { dietary: DietaryResult }) { const knownTags = Object.entries(dietary.tags).filter(([, value]) => value !== undefined); return <section aria-labelledby="dietary-heading" className="rounded-[32px] border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.22em] text-emerald-700">Dietary result</p><h2 id="dietary-heading" className="mt-2 text-2xl font-semibold text-slate-950">Can I eat this?</h2>{knownTags.length === 0 && dietary.allergens.length === 0 ? <p className="mt-4 text-sm leading-6 text-slate-600">Unknown — this USDA generic-food record has no ingredient or allergen statement.</p> : <div className="mt-5 flex flex-wrap gap-2.5">{knownTags.map(([key, value]) => <span key={key} className={`rounded-full px-4 py-2 text-sm font-semibold ${value ? 'bg-emerald-100 text-emerald-900' : 'bg-rose-100 text-rose-900'}`}>{value ? '✓' : '×'} {labels[key as keyof typeof labels]}</span>)}{dietary.allergens.map((allergen) => <span key={allergen} className="rounded-full bg-amber-100 px-4 py-2 text-sm font-semibold text-amber-950">⚠ Contains {allergen}</span>)}</div>}</section> }
