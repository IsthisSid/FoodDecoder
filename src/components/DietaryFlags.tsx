import type { DietaryEvidence, DietaryProfile } from '../data/dietaryEvidence'

const statusStyle = { contains: 'bg-rose-100 text-rose-950', 'may-contain': 'bg-amber-100 text-amber-950', 'potential-trigger': 'bg-amber-100 text-amber-950', 'no-listed-ingredient': 'bg-slate-100 text-slate-800', unknown: 'bg-slate-100 text-slate-700' }
const statusLabel = { contains: 'Contains', 'may-contain': 'May contain', 'potential-trigger': 'Potential trigger', 'no-listed-ingredient': 'No listed ingredient', unknown: 'Unknown' }
export function DietaryFlags({ evidence, profiles }: { evidence: DietaryEvidence[]; profiles: DietaryProfile[] }) { const visible = evidence.filter((item) => profiles.includes(item.profile)); return <section aria-labelledby="dietary-heading" className="rounded-[32px] border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.22em] text-emerald-700">Dietary evidence</p><h2 id="dietary-heading" className="mt-2 text-2xl font-semibold text-slate-950">What does the label show?</h2>{visible.length === 0 ? <p className="mt-4 text-sm text-slate-600">Choose one or more dietary concerns to see matching evidence.</p> : <ul className="mt-5 space-y-3">{visible.map((item) => <li key={item.profile} className="rounded-2xl border border-slate-100 p-4"><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-3 py-1 text-xs font-bold ${statusStyle[item.status]}`}>{statusLabel[item.status]}</span><span className="font-semibold text-slate-950">{item.profile.replace('-', ' ')}</span>{item.matchedText ? <span className="text-sm text-slate-700">Matched: “{item.matchedText}”</span> : null}</div><p className="mt-2 text-sm text-slate-700">{item.reason}</p><p className="mt-1 text-xs text-slate-500">Source: {item.source.label} · reviewed {item.source.reviewedDate}</p></li>)}</ul>}<p className="mt-5 text-xs leading-5 text-slate-500">Food Decoder reports available dataset and label evidence. It does not verify cross-contact or replace medical guidance.</p></section> }
