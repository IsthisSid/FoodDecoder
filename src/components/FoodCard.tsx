import type { DecodedFood } from '../api/foodApi'
export function FoodCard({ food }: { food: DecodedFood }) { return <section className="rounded-[32px] border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.22em] text-emerald-700">Decoded food</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-slate-950">{food.name}</h1><p className="mt-2 text-slate-600">{food.category || 'USDA food record'}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-800">Local USDA data</span></div><div className="mt-6 border-t border-slate-100 pt-4"><p className="text-xs font-bold uppercase tracking-[0.18em] text-slate-500">Sources</p><ul className="mt-2 space-y-1 text-sm text-slate-700">{food.sources.map((source) => <li key={source.id}>{source.label} · release {source.releaseDate}</li>)}</ul></div></section> }
