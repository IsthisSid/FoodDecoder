import { dietaryProfiles, type DietaryProfile } from '../data/dietaryEvidence'
export function DietaryProfilePicker({ selected, onChange }: { selected: DietaryProfile[]; onChange: (profiles: DietaryProfile[]) => void }) { const toggle = (profile: DietaryProfile) => onChange(selected.includes(profile) ? selected.filter((item) => item !== profile) : [...selected, profile]); return <fieldset className="rounded-[28px] border border-slate-200 bg-white p-5"><legend className="px-2 text-sm font-bold uppercase tracking-[0.18em] text-slate-600">My dietary concerns</legend><div className="mt-3 flex flex-wrap gap-2">{dietaryProfiles.map((profile) => <button key={profile.id} type="button" aria-pressed={selected.includes(profile.id)} onClick={() => toggle(profile.id)} className={`rounded-full px-3 py-2 text-sm font-medium ${selected.includes(profile.id) ? 'bg-slate-950 text-white' : 'bg-slate-100 text-slate-700'}`}>{profile.label}</button>)}</div></fieldset> }
